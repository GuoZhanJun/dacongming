#define _CRT_SECURE_NO_WARNINGS 1
//^按位与或
#include<stdio.h>
int main()
{
	//^（二进制）按位异或
	int a = 3;
	int b = 5;
	printf("变换前：a=%d b=%d\n", a, b);
	a = a ^ b;
	b = a ^ b;
	a = a ^ b;
	printf("变换后：a=%d b=%d\n", a, b);
	return 0;
}
